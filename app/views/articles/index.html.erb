<div class="nav">
  <h1>Articles</h1>
  <%= link_to 'Create new article', 'articles/new' %>
</div>

<% @articles.each do |article| %>
  <div class="article">
    <%= link_to article.title, "/articles/#{article.id}", :class => "title" %>
    <p><%= article.content %></p>
  </div>
<% end %>
